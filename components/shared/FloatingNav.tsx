import React from 'react';

const FloatingNav: React.FC = () => {
  // This component is not currently in use and has been replaced by the Header component.
  // It returns null to prevent any output.
  return null;
};

export default FloatingNav;
